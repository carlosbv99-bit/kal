# Instalación completa de kal (core + multimodal + dev). Si solo hace
# falta el Kernel de chat sin capacidades multimodales (por ejemplo, un
# entorno 100% cloud), instalar requirements-core.txt solo.
#
# Split introducido para que CI (y cualquier despliegue liviano) no tenga
# que descargar el stack de ML pesado (diffusers/accelerate/piper-tts/
# faster-whisper/rembg/playwright/sentence-transformers/moviepy) cuando no
# lo necesita — ver requirements-multimodal.txt.

-r requirements-core.txt
-r requirements-multimodal.txt
-r requirements-dev.txt
